# Generated by Skyramp v1.3.13 on 2026-03-16 23:08:43.909531158 +0000 UTC m=+149.807375282
# Command: skyramp generate contract rest http://localhost:8000/api/v1/usage-metrics/ \
# 		--framework pytest \
# 		--language python \
# 		--method GET \
# 		--output test_usage_metrics_list_contract.py \
# 		--output-dir /home/runner/work/flagsmith-testb/flagsmith-testb/tests \
# 		--overwrite \
# 		--provider \
# 		--response-status-code 200 \
# 		--runtime local \
# 		--skipProvisionParents

# Import of required libraries
import skyramp
import os
import time


# URL for test requests
URL = skyramp.get_base_url("SKYRAMP_TEST_BASE_URL", "http://localhost:8000")

# contract test for /api/v1/usage-metrics/ GET
def test_usage_metrics_get():
    # Invocation of Skyramp Client
    client = skyramp.Client()
    # Definition of authentication header
    headers = {}
    if os.getenv("SKYRAMP_TEST_TOKEN") is not None:
        headers["Authorization"] = "Bearer " + os.getenv("SKYRAMP_TEST_TOKEN")

    # Execute Request
    usage-metrics_GET_response = client.send_request(
        url=URL,
        path="/api/v1/usage-metrics/",
        method="GET",
        headers=headers
    )

    # Generated Assertions
    assert usage-metrics_GET_response.status_code == 200


if __name__ == "__main__":
    test_usage_metrics_get()
